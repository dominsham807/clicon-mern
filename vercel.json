{
    "buildCommand": "cd clicon-website && npm install && npm run build",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "rewrites":  [
        {"source": "/clicon-server/(.*)", "destination": "/clicon-server/index.js"}
    ]
}